---
- name: Provision Production K3s Cluster
  hosts: k3s_cluster
  become: true
  roles:
    - k3s
# too heavy for my pi
#- name: Deploy ArgoCD
#  hosts: localhost
#  connection: local
#  vars:
#    ansible_python_interpreter: "{{ ansible_playbook_python }}"
#  roles:
#    - argocd
